kivy==2.3.1
kivymd==1.2.0
reportlab==4.2.5
matplotlib==3.9.2
pillow==10.4.0

# O matplotlib puxa o numpy de qualquer forma; fixar a versão mantém o
# empacotamento reproduzível, porque a compatibilidade entre numpy e
# PyInstaller depende do par de versões (veja requirements-dev.txt).
numpy==2.5.3
