-r requirements.txt
pytest==8.3.3

# PyInstaller 6.11 não empacota corretamente o numpy 2.5: o módulo C
# multiarray acaba carregado duas vezes e o executável morre com
# "ImportError: cannot load module more than once per process".
# Corrigido a partir da 6.22.
pyinstaller>=6.22.2
